<div class="proposal_wrapper">
  <div class="row">
    <div class="col"> 
      <div class="row proposal_topbar">
        <div class="col m4 s12">
          <h1 class="proposal_title">Received Submissions </h1>
        </div>
        <div class="col m8 s12" style="display: grid">
          <ul class="br_tabs tabs hide-on-small-only "> 
            <li class="tab col m3 s12"><a href="/offers/mySubmissions">Submissions</a></li>
            <li class="tab col m3 s12"><a class="toggleRowClick active" href="/offers/receivedSubmissions">Received</a></li>
            <li class="tab col m3 s12"><a href="/offers/archivedSubmissions">Archived</a></li>
            {{!-- <li class="tab col m3 s12"><a class="toggleRowClick" href="#accepted">Accepted</a></li>
            <li class="tab col m2 s12"><a class="toggleRowClick" href="#declined">Declined</a></li> --}}
            <li class="tab col m3 s12"><a href="/offers/myOffers">Offers</a></li>
          </ul>
          <!-- Dropdown Trigger -->
          <div class="hide-on-med-and-up" style="display: grid;">
            <a class='dropdown-trigger btn' href='#' data-target='dropdown1'>Filter: <span id="mobileFilter">All</span></a>

            <!-- Dropdown Structure -->
            <ul id='dropdown1' class='dropdown-content'>
            <li class="tab col m2 s12"><a href="/offers/mySubmissions">Submissions</a></li>
            <li class="tab col m3 s12"><a class="toggleRowClick active" href="/offers/receivedSubmissions">Received</a></li>
            <li class="tab col m3 s12"><a href="/offers/archivedSubmissions">Archived</a></li>

            {{!-- <li class="tab col m3 s12"><a class="toggleRowClick" href="#accepted">Accepted</a></li>
            <li class="tab col m2 s12"><a class="toggleRowClick" href="#declined">Declined</a></li> --}}
            <li class="tab col m2 s12"><a href="/offers/myOffers">Offers</a></li>
            </ul>
          </div>
        </div>  
        <div class="col m2 s12" style="display: grid">
          <div class="proposal_title-wrapper">
            <a class="btn brBlue pull-right-md" style="display: grid;" href="/offers/addOffer">+ New Offer</a>
          </div>
        </div>
      </div> 
      {{!-- <div class="row">
        <div class="col m12 s12 gridonmobile" style="padding-bottom: 10px;">
          <div id='card' class="card" style="height: fit-content;margin: 0px !important">
            <select id="filteroffers" multiple class="select1 browser-default">
              {{#each tags}}
              <option value="{{this}}">{{this}}</option>
              {{/each}}
            </select>
          </div>
        </div> 
      </div>  --}}
      <div class="row">
        <div class="col s12 hide-on-small-only">
          <div class="">
            <div class="proposal_filter">
              <div class="col m1 s12">
              </div>
              <div class="col m1 s12">
                <p>Prize</p>
              </div>
              <div class="col m2 s12">
                <p>IG Username</p>
              </div>
              <div class="col m2 s12">
                <p>Status</p>
              </div>
              <div class="col m2 s12">
                <p>Last Comment</p>
              </div>
              <div class="col m1 s12">
                <p>Created</p>
              </div>
              {{!-- <div class="col m1 s12">
                <p>Amount</p>
              </div> --}}
              {{!-- <div class="col m2 s12">
                Tags
              </div> --}}
              <div class="col m4 s12">
                Actions
              </div>
            </div>
          </div>
        </div>
      </div>
      <span id="proposalTable">
        <div class="proposal-row row" id="accepted">
          {{#each offers}}
            {{#ifEquals votes.[0].voteBody "Accept"}}
              <div class="col s12">
                <div class="">
                  <div class="proposal_item">
                    <div class="col m1 s12">
                      {{#if url}}
                      <img src="{{url}}media/?size=l" class="proposal_image" width="100%" alt="" />
                      {{else}}
                      <img src="{{user.image}}" width="100%" class="proposal_image">
                      {{/if}}
                    </div>
                    <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Prize</p>
                      <p>{{compensation}}</p>
                    </div>
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">IG Username</p>
                      <p>
                        {{igUsername}}
                      </p>
                    </div>
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Status</p>
                      <p>
                        {{status}}
                      </p>
                    </div>
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Last Comment</p>
                      <p>
                        {{#if comments.[0].commentDate}}
                        {{formatDate comments.[0].commentDate 'MMMM Do YYYY, h:mm'}}
                        {{/if}}
                      </p>
                    </div>
                    <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Created</p>
                      <p>{{formatDate date 'MMMM Do YYYY'}}</p>
                    </div>
                    {{!-- <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Amount</p>
                      <p class="proposal-text">{{compensation}}{{price}}</p>
                    </div> --}}
                    {{!-- <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Tags</p>
                      <p class="proposal-text">
                        {{#each tag}}
                        <div class="chip">
                          {{this.text}}
                        </div>
                        {{/each}}
                      </p>
                    </div> --}}
                    <div class="col m4 s12">
                      <div class="row">
                        <div class="col m-auto s4">
                          <a class="proposal-btn btn grey" href="/offers/showSubmission/{{id}}"><i class="fa fa-eye"></i></a>
                        </div>
                        <div class="col m-auto s4">
                        <form  method="post" action="/offers/approveBulkSubmission/{{id}}">
                            <div>
                              <input name="Approve" type="submit" value="Approve" class="btn brBlue">
                            </div>
                          </form>
                        </div>
                        <div class="col m-auto s4">
                          <form  method="post" action="/offers/archiveSubmission/{{id}}">
                            <div>
                              <input name="Archive" type="submit" value="Archive" class="btn brPink">
                            </div>
                          </form>
                        </div>
                        
                        {{!-- <div class="col m-auto s4">
                        <form action="/offers/{{id}}?_method=DELETE" method="post" id="delete-form">
                          <input type="hidden" name="_method" value="DELETE">
                          <button type="submit" class="proposal-btn btn red"><i class="fa fa-remove"></i></button>
                        </form>
                      </div> --}}
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            {{/ifEquals}} 
          {{/each }} 
        </div> 
        <div class="proposal-row row" id="declined">
          {{#each offers}}
            {{#ifEquals votes.[0].voteBody "Decline"}}
              <div class="col s12">
                <div class="">
                  <div class="proposal_item">
                    <div class="col m1 s12">
                      {{#if url}}
                      <img src="{{url}}media/?size=l" class="proposal_image" width="100%" alt="" />
                      {{else}}
                      <img src="{{user.image}}" width="100%" class="proposal_image">
                      {{/if}}
                    </div>
                    <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Prize</p>
                      <p>{{compensation}}</p>
                    </div>
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">IG Username</p>
                      <p>
                        {{igUsername}}
                      </p>
                    </div>                    
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Status</p>
                      <p>
                        {{status}}
                      </p>
                    </div>
                    <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Last Comment</p>
                      <p>
                        {{#if comments.[0].commentDate}}
                        {{formatDate comments.[0].commentDate 'MMMM Do YYYY, h:mm'}}
                        {{/if}}
                      </p>
                    </div>
                    <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Created</p>
                      <p>{{formatDate date 'MMMM Do YYYY'}}</p>
                    </div>
                    {{!-- <div class="col m1 s12">
                      <p class="proposal-label hide-on-med-and-up">Amount</p>
                      <p class="proposal-text">{{compensation}}{{price}}</p>
                    </div> --}}
                    {{!-- <div class="col m2 s12">
                      <p class="proposal-label hide-on-med-and-up">Tags</p>
                      <p class="proposal-text">
                        {{#each tag}}
                        <div class="chip">
                          {{this.text}}
                        </div>
                        {{/each}}
                      </p>
                    </div> --}}
                    <div class="col m4 s12">
                      <div class="row">
                        <div class="col m-auto s4">
                          <a class="proposal-btn btn grey" href="/offers/showSubmission/{{id}}"><i class="fa fa-eye"></i></a>
                        </div>
                        <div class="col m-auto s4">
                        <form  method="post" action="/offers/approveBulkSubmission/{{id}}">
                            <div>
                              <input name="Approve" type="submit" value="Approve" class="btn brBlue">
                            </div>
                          </form>
                        </div>
                        <div class="col m-auto s4">
                          <form  method="post" action="/offers/archiveSubmission/{{id}}">
                            <div>
                              <input name="Archive" type="submit" value="Archive" class="btn brPink">
                            </div>
                          </form>
                        </div>
                        {{!-- <div class="col m-auto s4">
                        <form action="/offers/{{id}}?_method=DELETE" method="post" id="delete-form">
                          <input type="hidden" name="_method" value="DELETE">
                          <button type="submit" class="proposal-btn btn red"><i class="fa fa-remove"></i></button>
                        </form>
                      </div> --}}
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            {{/ifEquals}} 
          {{/each }} 
        </div> 
        <div class="proposal-row active row" id="all">
          {{#each offers}} 
            <div class="col s12">
              <div class="">
                <div class="proposal_item">

                  <div class="col m1 s12">
                    {{#if url}}
                    <img src="{{url}}media/?size=l" class="proposal_image" width="100%" alt="" />
                    {{else}}
                    <img src="{{user.image}}" width="100%" class="proposal_image">
                    {{/if}}
                  </div>
                  <div class="col m1 s12">
                    <p class="proposal-label hide-on-med-and-up">Prize</p>
                    <p>{{compensation}}</p>
                  </div>
                  <div class="col m2 s12">
                    <p class="proposal-label hide-on-med-and-up">IG Username</p>
                    <p>
                      {{igUsername}}
                    </p>
                  </div>                  
                  <div class="col m2 s12">
                    <p class="proposal-label hide-on-med-and-up">Status</p>
                    <p>
                      {{status}}
                    </p>
                  </div>
                  <div class="col m2 s12">
                    <p class="proposal-label hide-on-med-and-up">Last Comment</p>
                    <p>
                      {{#if comments.[0].commentDate}}
                      {{formatDate comments.[0].commentDate 'MMMM Do YYYY, h:mm'}}
                      {{/if}}
                    </p>
                  </div>
                  <div class="col m1 s12">
                    <p class="proposal-label hide-on-med-and-up">Created</p>
                    <p>{{formatDate date 'MMMM Do YYYY'}}</p>
                  </div>
                  {{!-- <div class="col m1 s12">
                    <p class="proposal-label hide-on-med-and-up">Amount</p>
                    <p class="proposal-text">{{compensation}}{{price}}</p>
                  </div> --}}
                  {{!-- <div class="col m2 s12">
                    <p class="proposal-label hide-on-med-and-up">Tags</p>
                    <p class="proposal-text">
                      {{#each tag}}
                      <div class="chip">
                        {{this.text}}
                      </div>
                      {{/each}}
                    </p>
                  </div> --}}
                  <div class="col m4 s12">
                    <div class="row">
                      <div class="col m-auto s4">
                        <a class="proposal-btn btn grey" href="/offers/showSubmission/{{id}}"><i class="fa fa-eye"></i></a>
                      </div>
                      <div class="col m-auto s4">
                        <form  method="post" action="/offers/approveBulkSubmission/{{id}}">
                          <div>
                            <input name="Approve" type="submit" value="Approve" class="btn brBlue">
                          </div>
                        </form>
                      </div>
                      <div class="col m-auto s4">
                        <form  method="post" action="/offers/archiveSubmission/{{id}}">
                          <div>
                            <input name="Archive" type="submit" value="Archive" class="btn brPink">
                          </div>
                        </form>
                      </div>
                      

                      {{!-- <div class="col m-auto s4">
                      <form action="/offers/{{id}}?_method=DELETE" method="post" id="delete-form">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="proposal-btn btn red"><i class="fa fa-remove"></i></button>
                      </form>
                    </div> --}}
                    </div>
                  </div>
                </div>
              </div>
            </div> 
            {{else}}
            
            <div>
              <div class="center-align" style="padding:20px;">
                  <h5>You haven't received any submissions for your offers yet :( <br><br> 
                  Create an offer if you haven't, award something enticing, and knock on your customers' doors to let them know you want to give them a prize!</h5>
              </div>
          </div>
          {{/each }} 
        </div> 
      </span>
    </div>
  </div>
</div>